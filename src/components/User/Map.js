import React from 'react';

function Map({ hotels }) {
  // Implement map functionality using Google Maps API or any other map library of your choice

  return (
    <div style={{ maxWidth: '600px', margin: '0 auto' }}>
      <h2 style={{ fontSize: '24px', marginBottom: '20px' }}>Map</h2>
      {/* Render the map component here */}
    </div>
  );
}

export default Map;